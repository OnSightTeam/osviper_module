{% include 'header' %}

// Frameworks
#import <XCTest/XCTest.h>
#import <OCMock/OCMock.h>

// Classes
#import "{{ prefix }}{{ module_info.name }}Interactor.h"

#import "{{ prefix }}{{ module_info.name }}InteractorOutput.h"


@interface {{ prefix }}{{ module_info.name }}InteractorTests : XCTestCase

@property (nonatomic, strong) {{ prefix }}{{ module_info.name }}Interactor* interactor;

@property (nonatomic, strong) id mockOutput;

@end

@implementation {{ prefix }}{{ module_info.name }}InteractorTests


#pragma mark - Setup environment of the testing - 

- (void) setUp 
{
    [super setUp];

    self.interactor = [[{{ prefix }}{{ module_info.name }}Interactor alloc] init];

    self.mockOutput = OCMProtocolMock(@protocol({{ prefix }}{{ module_info.name }}InteractorOutput));

    self.interactor.output = self.mockOutput;
}

- (void) tearDown 
{
    self.interactor = nil;

    self.mockOutput = nil;

    [super tearDown];
}

#pragma mark - Testing methods {{ prefix }}{{ module_info.name }}InteractorInput -

@end
